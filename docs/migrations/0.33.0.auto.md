<!-- 生成物: bin/fge migration-notes が作る。手で編集しない（make bump で作り直す。手当てを足すなら docs/migrations/0.33.0.md へ） -->

# engine 0.32.0 → 0.33.0 への追随

## 直す物（2 件）

### 1. DrawCmd.BoxStyle（engine）

- 消えた中身: cornerRadius, borderWidth, borderColor, borderAlpha, stripeColor, stripeAlpha, stripeWidth, stripePeriod, stripeDir, checkerColor, checkerAlpha, checkerCell
- 増えた中身: BoxStyle

- 前: `pub type alias BoxStyle = { cornerRadius = Float64, borderWidth = Float64, borderColor = Color, borderAlpha = Float64, stripeColor = Color, stripeAlpha = Float64, stripeWidth = Float64, stripePeriod = Float64, stripeDir = Int32, checkerColor = Color, checkerAlpha = Float64, checkerCell = Float64 }`
- 後: `pub enum BoxStyle { case BoxStyle( Float64, Float64, Float64, Float64, Float64, Float64, Float64, Float64, Float64, Float64, Float64, Float64, Int32, Float64, Float64, Float64, Float64, Float64 ) }`

engine の中に追随例がありません。手当ては自分で考えてください。

### 2. DrawCmd.Drawable（engine）

- 消えた中身: texture, position, scale, rotation, color, alpha, centered, uvOffset, uvScale, zIndex, style, clip, blend, mask
- 増えた中身: Drawable

- 前: `pub type alias Drawable = { texture = String, position = Vec2.Vec2, scale = Vec2.Vec2, rotation = Float64, color = Color, alpha = Float64, centered = Bool, uvOffset = Vec2.Vec2, uvScale = Vec2.Vec2, zIndex = Int32, style = Option[BoxStyle], clip = Option[Rect2.Rect2], blend = BlendMode, mask = List[List[Vec2.Vec2]] }`
- 後: `pub enum Drawable { case Drawable( String, Float64, Float64, Float64, Float64, Float64, Float64, Float64, Float64, Float64, Bool, Float64, Float64, Float64, Float64, Int32, Option[BoxStyle], Option[Rect2.Rect2], BlendMode, List[List[Vec2.Vec2]] ) }`

engine の中に追随例がありません。手当ては自分で考えてください。

## 増えた宣言（32 件・読むだけでよい）

- `DrawCmd.BoxStyleSpec`（engine）
- `DrawCmd.DrawableSpec`（engine）
- `DrawCmd.alphaOf`（engine）
- `DrawCmd.blendOf`（engine）
- `DrawCmd.borderWidthOf`（engine）
- `DrawCmd.clipOf`（engine）
- `DrawCmd.colorOf`（engine）
- `DrawCmd.fromParts`（engine）
- `DrawCmd.make`（engine）
- `DrawCmd.makeStyle`（engine）
- `DrawCmd.maskOf`（engine）
- `DrawCmd.positionOf`（engine）
- `DrawCmd.rotationOf`（engine）
- `DrawCmd.scaleOf`（engine）
- `DrawCmd.styleFromParts`（engine）
- `DrawCmd.styleOf`（engine）
- `DrawCmd.styleToSpec`（engine）
- `DrawCmd.textureOf`（engine）
- `DrawCmd.toSpec`（engine）
- `DrawCmd.withBorder`（engine）
- `DrawCmd.withChecker`（engine）
- `DrawCmd.withClip`（engine）
- `DrawCmd.withCornerRadius`（engine）
- `DrawCmd.withPositionAndRotation`（engine）
- `DrawCmd.withScale`（engine）
- `DrawCmd.withScaleAndMask`（engine）
- `DrawCmd.withStripe`（engine）
- `DrawCmd.withTintAndZIndex`（engine）
- `DrawCmd.withUv`（engine）
- `DrawCmd.zIndexOf`（engine）
- `ShaderGen.eqSpec`（engine）
- `ActiveDocs.fingerprint`（engine_world）

## 診られない物

schema が無いので、この種の Doc の非互換はこの一覧に出ません: shader, theme, level, hitbox

render_gl は見ていません（ゲームが直接呼ばない層）。
