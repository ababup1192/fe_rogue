{
  "note": "gen-rules（docs/ の規約から .claude/rules/*.md を作る）の規約データ。source of truth は bin/gen-rules.py で、この JSON はそこに書いてある値を一字一句そのまま写した物。Go 版（bin/fge-go gen-rules）はこれを読んで動くので、規約を変えるときは bin/gen-rules.py とこの 2 つを同時に直す。ずれると go/internal/genrules の TestRulesMatchPython が落ちる。rules は並びが出力の行順になるので配列で持つ（JSON のオブジェクトは並びを保てない）。",
  "banner": "<!-- generated by bin/gen-rules.py from {src} — 編集しないで。本文は {src} を直して make rules -->",
  "rules": [
    {
      "out": "drawing.md",
      "src": "docs/drawing-floor.md",
      "globs": [
        "**/View*.flix",
        "**/src/render/*.flix",
        "**/src/hand/*.flix",
        "**/*Hand.flix",
        "**/*Scene.flix",
        "**/*Map.flix",
        "**/*.sprite.json",
        "**/*.theme.json",
        "**/*.palette.json",
        "**/*.fx.json",
        "**/*.shader.json"
      ]
    },
    {
      "out": "flix.md",
      "src": "docs/flix-conventions.md",
      "globs": ["**/*.flix"]
    },
    {
      "out": "error-handling.md",
      "src": "docs/error-handling.md",
      "globs": ["**/*.flix"]
    }
  ],
  "outDirs": [".claude/rules", "agents-pack/rules"]
}
