[package]
name        = "flix_game_engine_ide"
description = "Standalone IDE for flix_game_engine projects (Swing + AWTGLCanvas based scene editor)"
version     = "0.1.0"
flix        = "0.72.0"
authors     = ["ababup1192 <ababup1192@gmail.com>"]

[dependencies]
"github:mlutze/flix-json" = "0.13.2"
# engine 経由で render_core + engine_core を transitive に取得する。
# SceneLoader / Scene / GameEngine 型などを使うため engine 自体に依存する必要がある。
"github:ababup1192/flix_game_engine" = { version = "0.1.0", security = "unrestricted" }

[mvn-dependencies]
"com.formdev:flatlaf"   = "3.7.1"
# AWTGLCanvas (lwjgl3-awt) と JAWT バインディング。LWJGL の他のモジュール
# (lwjgl base / opengl / stb / openal / glfw) は engine 経由で transitive に降りてくる。
"org.lwjglx:lwjgl3-awt" = "0.2.4"
"org.lwjgl:lwjgl-jawt"  = "3.4.1"
