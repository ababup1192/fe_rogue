[package]
name        = "flix_game_engine"
description = "Reusable 2D game engine"
version     = "0.1.0"
flix        = "0.75.1"
authors     = ["ababup1192 <ababup1192@gmail.com>"]

[dependencies]
# engine は GL 非依存のフロント（Game/Audio 契約・シーングラフ・Resource）。
# ネイティブは持たず、描画の実体はバックエンド（render_gl など）が実装する。
"github:ababup1192/flix_engine_core" = { version = "0.1.0", security = "unrestricted" }
