{
  "//": "town.map.json の形の宣言（sections 方言）。街のタイル盤: legend が rows の 1 文字をタイル名（town_*.sprite.json のスプライト名）に写し、rows が敷き方を決める。知らない文字のマスは敷かれない（落ちない）。縁・バリエーションの選定はコード側。",
  "version": 1,
  "sections": {
    "note": { "kind": "field", "type": "text", "label": "メモ", "order": 1, "widget": "multiline" },

    "rows": { "kind": "field", "type": "grid", "label": "街の敷き方（'g'=芝 'p'=石畳 'w'=水路 '#'=漆喰壁。legend で変えられる）", "order": 10 },

    "legend": { "kind": "field", "type": "json", "label": "文字 → タイル名の対応（town_*.sprite.json のスプライト名）", "order": 20 }
  }
}
