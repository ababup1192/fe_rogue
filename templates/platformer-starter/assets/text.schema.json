{
  "//": "ui.text.json の形の宣言（Studio の sections 方言）。既定値は src/UiText.flix の defaults() が正。フォントは英数字だけ焼いてあるので、日本語にするなら project.json の fonts を差し替える。",
  "version": 1,
  "sections": {
    "clear":    { "kind": "field", "type": "text", "label": "面をクリアしたときの一言", "group": "画面に出る文言", "order": 1, "default": "CLEAR!" },
    "gameOver": { "kind": "field", "type": "text", "label": "力尽きたときの一言", "group": "画面に出る文言", "order": 2, "default": "GAME OVER" }
  }
}
