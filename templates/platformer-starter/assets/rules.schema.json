{
  "//": "platformer.rules.json の形の宣言（Studio の sections 方言）。既定値は src/RulesDoc.flix の defaults() が正。規則（いつ加点するか・いつ場面が変わるか）はコード（World）側で、ここは数値だけ。group は見せ方だけの束ね（同じ group 名が 1 枚のタブになる）。",
  "version": 1,
  "sections": {
    "note": { "kind": "field", "type": "text", "label": "メモ", "widget": "multiline", "group": "収支", "order": 0 },

    "coinScore":  { "kind": "field", "type": "int", "label": "コイン 1 枚の点（集める楽しさの単価）", "group": "収支", "order": 1, "min": 0, "max": 10000, "default": 100 },
    "stompScore": { "kind": "field", "type": "int", "label": "敵を 1 匹踏んだ点（大きいほど戦う遊びが得）", "group": "収支", "order": 2, "min": 0, "max": 10000, "default": 200 },
    "goalScore":  { "kind": "field", "type": "int", "label": "旗に触れた点（完走そのものの褒美）", "group": "収支", "order": 3, "min": 0, "max": 10000, "default": 500 },

    "maxHearts":    { "kind": "field", "type": "int",   "label": "ハートの数（開始時の残機。HUD の並びもこれで増える）", "group": "進行", "order": 10, "min": 1, "max": 9, "default": 3 },
    "clearWaitSec": { "kind": "field", "type": "float", "label": "CLEAR! の幕を見せる秒（短いほどテンポよく次の面へ）", "unit": "秒", "group": "進行", "order": 11, "min": 0.1, "max": 8, "step": 0.1, "default": 1.6 },
    "overWaitSec":  { "kind": "field", "type": "float", "label": "GAME OVER の幕を見せる秒（この後 1 面目からやり直し）", "unit": "秒", "group": "進行", "order": 12, "min": 0.1, "max": 8, "step": 0.1, "default": 2.0 }
  }
}
