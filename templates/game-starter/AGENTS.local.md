# このゲーム固有の設計原則

（ここに、このゲームだけの原則を書く。共通の作法はエンジン側の AGENTS.core.md が
sync で AGENTS.md に入るので、重複して書かない。書いたら engine 側で
`make sync-agents GAME=<このフォルダ>` を再実行して AGENTS.md に反映する。）

例:
- 画面に文字を出さない。すべて絵と音で伝える。
- プレイヤーは死なない。失敗は時間の損だけで表す。
