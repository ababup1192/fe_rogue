{
  "//": "race.course.json の形の宣言（sections 方言）。rows は文字格子（1 行 = コースを縦に切った 1 段。上ほど先、下ほど手前）。'#' = 路面（走れる）、'.'/空白 = 草（道の外・減速）。'#' の帯を左右にずらすとカーブ、幅を変えると道の広さが変わる。コースは下から上へ無限に流れる（周回）。曲がる・はみ出す・周回するの規則はコード（World）側。",
  "version": 1,
  "sections": {
    "note": { "kind": "field", "type": "text", "label": "メモ", "order": 1, "widget": "multiline" },
    "rows": { "kind": "field", "type": "grid", "label": "コースの段（上ほど先）", "order": 2 }
  }
}
