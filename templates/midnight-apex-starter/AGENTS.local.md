# MIDNIGHT APEX 固有の設計原則

- 操作の核はアクセル・ブレーキ・ハンドルの3つに絞る。
- 道路の表示と路面判定は、必ず `CourseDoc.roadAt` の同じ結果を使う。
- 速度、収支、色、コース、画面文言は Doc、進行と判定はコードに置く。
- 夜道の読みやすさを優先し、路肩、自機、ライバル、警告は色の役割を混ぜない。
- 演出の座標はテストせず、タイトル・走行・路外・ゴールの golden で目視する。
