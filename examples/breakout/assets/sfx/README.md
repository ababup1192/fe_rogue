# assets/sfx

このフォルダの `.wav` は手で置いた録音ではなく、`make bake` が `src/SfxBake.flix` から生成する成果物です。
音を変えたい・足したいときは `.wav` を直接編集せず、`SfxBake.flix` の合成レシピを編集して `make bake` し直してください。
（音の追加手順の全体は `README.md` の練習課題④を参照）
